import { CamelToSpacePipe } from './camel-to-space.pipe';

describe('CamelToSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new CamelToSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
